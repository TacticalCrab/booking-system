import { browser } from '$app/environment'; import * as api from '$lib/api/auth'; import type { User } from '$lib/api/types';
const ACCESS = 'booking_access_token'; const REFRESH = 'booking_refresh_token'; const USER = 'booking_user';
class AuthState { accessToken = $state<string | null>(browser ? localStorage.getItem(ACCESS) : null); refreshToken = $state<string | null>(browser ? localStorage.getItem(REFRESH) : null); user = $state<User | null>(browser ? JSON.parse(localStorage.getItem(USER) ?? 'null') : null); ready = $state(false); get isAdmin() { return this.user?.role === 'ADMIN'; } setSession(accessToken: string, refreshToken: string, user: User) { this.accessToken = accessToken; this.refreshToken = refreshToken; this.user = user; if (browser) { localStorage.setItem(ACCESS, accessToken); localStorage.setItem(REFRESH, refreshToken); localStorage.setItem(USER, JSON.stringify(user)); } } clear() { this.accessToken = null; this.refreshToken = null; this.user = null; if (browser) { localStorage.removeItem(ACCESS); localStorage.removeItem(REFRESH); localStorage.removeItem(USER); } } async refresh() { if (!this.refreshToken) { this.clear(); return false; } try { const tokens = await api.refresh(this.refreshToken); this.accessToken = tokens.accessToken; this.refreshToken = tokens.refreshToken; if (browser) { localStorage.setItem(ACCESS, tokens.accessToken); localStorage.setItem(REFRESH, tokens.refreshToken); } return true; } catch { this.clear(); return false; } } async restore() { if (this.accessToken) { try { this.user = await api.me(); if (browser) localStorage.setItem(USER, JSON.stringify(this.user)); } catch { await this.refresh(); if (this.accessToken) try { this.user = await api.me(); } catch { this.clear(); } } } this.ready = true; } async logout() { const token = this.refreshToken; this.clear(); if (token) try { await api.logout(token); } catch { } } }
export const auth = new AuthState();
